apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-python-api.fullname" . }}-test-connection"
  labels:
    {{- include "sample-python-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-python-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
